// && -> and
// || -> or
// ! -> reverse (if true the false and if false then true)

let isLoggedIn = true;
let isPaid = false;

console.log(isLoggedIn && isPaid);

let isEmailUser = true;
let isGoogleUser = false;

console.log(isEmailUser || isGoogleUser);